package com.rais.nexusbody.core.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * NEXUS BODY UI CONSTANTS
 * Peran: Sumber tunggal untuk seluruh variabel warna aplikasi.
 * Hubungan: Digunakan oleh seluruh file UI (Screen & Components).
 */
val textprimary = Color(0xFFFFFFFF) // Putih
val textsecondary = Color(0xB3FFFFFF) // Abu-abu terang
val textmuted = Color(0x80FFFFFF) // Abu-abu redup
val premiumaccent = Color(0xFFE65100) // Oranye identitas
val statusgood = Color(0xFF34C759) // Hijau
val statuswarning = Color(0xFFFFCC00) // Kuning
val statusdanger = Color(0xFFFF3B30) // Merah
